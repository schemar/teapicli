import React, { FunctionComponent } from "react";
import { Box } from "ink";

const ResponseComponent: FunctionComponent<{}> = () => {
  return <Box padding={1}>Response:</Box>;
};

export default ResponseComponent;
